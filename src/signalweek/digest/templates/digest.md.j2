# Signalweek digest

For **{{ digest.user_email }}**, week of {{ digest.window_start | fmt_date }} to {{ digest.window_end | fmt_date }}.

{% if digest.is_empty -%}
_No signals this week._
{% else -%}
{% for section in digest.sections -%}
## [{{ section.source_title }}]({{ section.source_url }})

{% for item in section.items -%}
- [{{ item.title }}]({{ item.url }}){% if item.published_at %} — {{ item.published_at | fmt_datetime }}{% endif %}
{% if item.summary %}  {{ item.summary }}
{% endif -%}
{% endfor %}
{% endfor -%}
{% endif -%}
